// El operador ternario es una simplificacion del bloque else if

let miNumero = 0;

// operador ternario, primero va la condicion, luego lo que pasara se la condicion es positiva y luego si es negativa

(miNumero>0) ? console.log(`Positivo`) : console.log(`Negativo`);

