// Areglos en JS
// definir un arreglo

let numerosArreglos= [];

// modificar los valores

numerosArreglos[0]= 13;
numerosArreglos[1]= 20;
numerosArreglos[4] = 'Hola';

// Leer los valores del arreglo

for(let a=0; a<=4;a++){
    console.log(`indix #${a} = ${numerosArreglos[a]}`);
}

let colores =['rojo','azul','verde','blanco'];

for(let color =0; color < colores.length; color ++){
    console.log(colores[color]);
}

