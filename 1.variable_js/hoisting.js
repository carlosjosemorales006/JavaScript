// Hoisting en js es el proceso por el cual las declaraciones de variables son usadas antes de que sean declaradas.

let x; //1.  Declaración de variable
 x = 10;  // 2. Inicialización de variable
y = s;
 console.log(y); 

 var y;
var s ;

// este consecto es confuso y no se utiliza
