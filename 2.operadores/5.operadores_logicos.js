let a = true;
let b = false;

console.log(a);
console.log(b)

// Operador logico && (AND o Y)

console.log(`${a} && ${b} -> ${a && b}`)

// operador logico || (OR o O)

console.log(`${a} || ${b} -> ${a || b}`)

// operador logico not (!)

console.log(`${a} -> ${!a}`)